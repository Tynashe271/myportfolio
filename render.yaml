services:
  - type: web
    name: myportfolio
    runtime: go
    repo: https://github.com/YOUR_USERNAME/myportfolio
    plan: free
    region: oregon
    buildCommand: "go build -o portfolio-server ."
    startCommand: "./portfolio-server"
